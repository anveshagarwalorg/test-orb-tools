version: 2.1

description: |
  A simple "Hello, World!" orb

commands:
  expire:
    steps:
      - run:
          name: Echo Hello world
          command: |
            echo "Hello World !"
            echo "fourth release"
            echo "version 0.0.6"
